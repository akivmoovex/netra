<div class="container">
  <div class="footer">
    <div style="display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;">
      <div>
        <strong>Netraz</strong>
        <div>From invisible to in-demand: we build digital systems that bring clients to you.</div>
      </div>
      <div>
        <div><span class="muted">Call Center:</span> <%= netraPhone %></div>
        <% if (netraAddress) { %>
          <div><span class="muted">Location:</span> <%= netraAddress %></div>
        <% } %>
        <% if (netraEmail) { %>
          <div><span class="muted">Email:</span> <a href="mailto:<%= netraEmail %>"><%= netraEmail %></a></div>
        <% } %>
      </div>
    </div>
  </div>
</div>

